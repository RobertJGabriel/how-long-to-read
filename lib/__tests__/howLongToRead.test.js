const assert = require('assert');
const howLongToRead = require('../index.js');

describe('howLongToRead', () => {
  it('has a test', () => {
    assert(false, 'howLongToRead should have a test');
  });
});
