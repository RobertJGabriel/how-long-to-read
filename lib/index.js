'use strict';

/**
 * @param  {} params
 */
function time(string) {
  if (string === null || string.trim() === '') {
    return 0; // Return 0 as it will take zero mins to read
  }
}

exports.time = time;
